const f = require('../if-06.js');

QUnit.test("ЗАДАЧА 06. количество общих точек у прямой и окружности", 
  function(assert){
	assert.deepEqual(f(1,1,1) 2, "Не выполнено f(1,1,1");
	assert.deepEqual(f(1,1,1) 2, "Не выполнено f(1,1,1");
  }
);
